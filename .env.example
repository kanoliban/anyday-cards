DB_URL=
DB_TOKEN=
DB_HOST=
DB_NAME=
DB_USER=
DB_PASSWORD=

NEXT_PUBLIC_HOST=
NEXT_PUBLIC_BUILD_TIME=

NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=

# ADC A/B testing (set to false to force v2 generation while tuning)
NEXT_PUBLIC_AB_TESTING_ENABLED=false

# Stripe
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# AI Generation (for card customization wizard)
# Note: use a standard project API key with model inference permission (scope: `model.request`).
# Admin/restricted keys without `model.request` will always return fallbacks.
OPENAI_API_KEY=
# Optional: override default generation model (defaults to gpt-4.1-mini)
OPENAI_MODEL=

# SendGrid (email delivery)
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=

# Lob (print & mail fulfillment)
LOB_API_SECRET=
